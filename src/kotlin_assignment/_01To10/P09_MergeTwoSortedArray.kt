package kotlin_assignment._01To10;

/*
 * program to merge the two sorted array into one
 */

// method to merge given two sorted arrays
fun mergeTheseArrays(arr1: Array<Int>, arr2 : Array<Int>){
    var arr1Count: Int = 0;
    var arr2Count: Int = 0;
    while(arr1.size >= 12){

    }
}